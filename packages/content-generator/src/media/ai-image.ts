/**
 * AI image generation for social media posts.
 * Optional integration with Flux, DALL-E, or similar.
 *
 * TODO: Implement when ready to add AI-generated visuals.
 * - Meme images with caption overlay
 * - Custom illustrations for learning content
 */

export function generateAiImage(_prompt: string): Buffer {
  throw new Error(
    "Not implemented yet — choose and configure an image generation provider"
  );
}
