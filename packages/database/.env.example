DATABASE_URL="postgresql://allonfire:allonfire_dev@localhost:5432/allonfire"

# Required: main admin with full access to all apps
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="changeme123"
# ADMIN_NAME="Admin"

# "prod" (default) = admin only, "dev" = admin + test users + mock data
# SEED_MODE="dev"
# TEST_PASSWORD="testpass123"
