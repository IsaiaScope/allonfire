// Stylesheets are imported for their side effect (`import "./globals.css"`);
// Next bundles them. noUncheckedSideEffectImports needs a declaration to know
// the import resolves.
declare module "*.css";
